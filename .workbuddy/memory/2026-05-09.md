## 修复 - 侧边栏导航菜单"用户中心/快速退出/管理后台"位置浮动问题

### 问题
侧边栏中"用户中心"、"快速退出"、"管理后台"这三个条目在扁平列表中位置会上浮，JS 注入的分隔线不稳定。

### 方案演进
1. **第一版**：改为 `st.navigation` 字典分组（"分析工具"组 + "系统"组）
   - 问题：产生了多余的 section 标题，用户不需要
2. **最终版**：恢复扁平列表 + 重写 `inject_nav_separator()`
   - 用 `MutationObserver` 替代 `setInterval`，更稳定
   - 只在"用户中心"条目前插入一条 `<li>` 分割线，且只插入一次（`dataset.stSep` 标记）
   - 8秒后自动断开 Observer
   - `styles.py` 中 `.nav-separator` CSS 已移除，改用 inline style

### 改动文件
- `数据管理.py`: 恢复扁平 pages 列表 + 恢复 `inject_nav_separator` 导入和调用
- `utils/styles.py`: 重写 `inject_nav_separator()`，移除废弃函数，新增 MutationObserver 版本
- `utils/visitor_logger.py`: 移除 `render_sidebar_stats()` 中访问统计上方的 `st.sidebar.markdown("---")` 分割线
