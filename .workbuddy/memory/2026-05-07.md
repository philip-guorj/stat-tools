# 2026-05-07 工作日志

## 代码质量优化（基于代码审查报告）

### 完成项

1. **新建 `utils/imports.py`** — 统一的延迟导入模块
   - 用 `st.cache_resource` 管理所有重量级库的懒加载
   - 提供：`get_statsmodels()`, `get_sm()`, `get_ols()`, `get_anova_lm()`, `get_tukey()`, `get_vif()`, `get_scipy_stats()`, `get_plotly_express()`, `get_plotly_go()` 等

2. **消除 5 处 `_get_statsmodels()` 重复定义** — 全部改为从 `utils.imports` 导入：
   - `pages/03_方差分析.py`
   - `pages/04_回归分析.py`（含 `_get_sm()` / `_get_vif()`）
   - `modules/combining.py`
   - `modules/ssr_analysis.py`
   - `modules/trial_report_analysis.py`

3. **修复 `pages/10_区试报告.py` 5 处函数内重复导入** — 改为使用 `utils.imports` 缓存版本

4. **修复 13 处 `bare except`** — 全部改为 `except Exception:`：
   - `modules/trial_report_analysis.py`：8 处
   - `pages/01_描述统计.py`：1 处
   - `pages/05_多变量分析.py`：1 处
   - `pages/06_数据可视化.py`：1 处
   - `launch.py`：1 处
   - `stattools_launcher.py`：1 处

5. **合并 `render_data_manager` + `render_data_manager_expanded`**
   - `render_data_manager(expanded=None)` 统一版本
   - `render_data_manager_expanded()` 改为委托调用 `render_data_manager(expanded=True)`
   - 消除 ~250 行重复代码

### 验证结果
- 所有 14 个修改文件通过语法检查
- 全部页面和模块导入验证通过，0 错误
